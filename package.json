{
  "name": "cc-notify",
  "version": "0.1.0",
  "description": "Claude Code 会话状态监控插件：菜单栏 + 系统通知",
  "private": true,
  "main": "app/main.js",
  "scripts": {
    "dev": "electron app/main.js",
    "setup": "bash scripts/install.sh",
    "test": "vitest run"
  },
  "dependencies": {
    "electron": "^43.3.0",
    "node-notifier": "^10.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
